<odoo>
    <record id="ir_cron_cancel_expired_chaitanya_reservations" model="ir.cron">
        <field name="name">Cancel Expired Appointment Reservations</field>
        <field name="model_id" ref="model_chaitanya_appointment_booking"/>
        <field name="state">code</field>
        <field name="code">model._cron_cancel_expired_reservations()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>